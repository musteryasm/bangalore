import React from 'react';
import { Typography } from '@mui/material';

const Units = () => {
  return (
    <Typography variant="body1">
      This is the Units component.
    </Typography>
  );
};

export default Units;
